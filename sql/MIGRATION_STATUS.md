# สถานะการรัน migration บน production (syvfdbvmwaeyokytckwb)

## migration_v8.4.sql

รันแล้ว **18 ก.ย. 2569** — เดิมไม่เคยถูกรันเลยทั้งไฟล์ ทำให้ปุ่ม "➕ เข้าร่วม"
บนบอร์ดเครื่องเด้ง "ยังไม่ได้อัปเดตฐานข้อมูล (migration v8.4)" ทุกครั้ง

### รันแล้ว ✅
| ส่วน | สิ่งที่ได้ |
|---|---|
| 1 | `production_ticket.workers` · `shape_drawing.sketch` · `shape_drawing.note` |
| 2-3 | RPC `join_work` / `leave_work` / `close_work` + grant ให้ anon, authenticated |
| 6 | `so_live.cancelled_at` / `cancelled_by` / `cancel_reason` + partial index |
| 8 | `so_live.ivnum` |

### ยังไม่รัน — รอ CTO เคาะ ⏸
| ส่วน | เหตุผลที่ข้าม |
|---|---|
| 5 | เปลี่ยนชื่อเครื่อง SKN เป็น "ตรง-L เขียว ปีกซ้าย" / "ตรง-R แดง ปีกขวา" — **เปลี่ยนชื่อคอลัมน์ที่คนหน้างานเห็นบนบอร์ด** ไม่ใช่สิ่งที่ CTO ขอในรอบนี้ |
| 7, 9 | เพิ่มเครื่อง "งานพับมือ" (3 สาขา) · "พับครอบ" + "ย้ำกันสาด" (SKN) — **เพิ่มคอลัมน์ใหม่บนบอร์ด** (พับครอบมีอยู่แล้วในระบบ) |

ทั้ง 3 ส่วนที่ข้ามไว้รันซ้ำได้ ไม่พัง — ถ้าจะเปิดใช้ ให้รันเฉพาะส่วนนั้นจากไฟล์ `migration_v8.4.sql`
